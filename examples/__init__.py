"""AI-GR worked examples.

Each example is a self-contained module that builds a GPR chain for a
realistic but synthetic AI system. Run them directly with
``python -m examples.<name>`` from the repo root.
"""
